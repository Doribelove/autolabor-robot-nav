# 覆盖导航实车实验记录（2026-09-04）

## 当前结论

2026-09-04 当前 TEB 固定档位架构的前两轮实车实验均未完成。首轮执行到第 3 条清扫线
之前的第二次区内转场时暂停；第二轮换到更长、更空旷的区域后，在第 1 条清扫线之后的
第一次区内转场就复现同一故障。两次都没有跳过清扫线。该失败不能归类为 Hybrid A*
搜索失败。Hybrid A* 在初始转场、cusp 后整段重算和入口异常恢复中均返回了路径；首先
暴露的是 TEB 在固定倒车 action 后半段反复生成正向候选并被硬档位门拒绝，随后才由
横向偏差门触发整段重规划。重规划得到的入口恢复路径又因累计前进距离超过现有防绕圈
上限而被覆盖管理器主动拒绝，最终有限重试耗尽并暂停。

两轮均没有由 Codex 修改 Qt 参数、发送导航目标、恢复任务或接管车辆；区域选择、任务
开始及人工恢复均由现场操作员在 Qt 完成。Codex 只订阅状态、速度、里程计、路径和日志。
当前第二轮任务保持暂停、底盘零速。第二轮空旷区域已经证明上一轮附近地图边界不是复现
该故障的必要条件；当前应先修复 TEB 固定档位执行和恢复候选选择，再继续重复运动实验。

## 环境与运行参数

- J6M release：`20260904_022107`；实验期间未重新部署；
- 静态地图：`global_maps/map_sets/latest` -> `map_20260827_184123`（fused）；
- 任务开始：2026-09-04 03:14:50.929 CST（J6M ROS 文件按 UTC 显示为
  2026-09-03 19:14:50.929）；
- 计划 ID：`a8399b3528094226ac60f3618cba3fdb`；
- 区域：28.004 m² 四边形，顶点依次为
  `(14.917,-35.667)`、`(18.584,-35.056)`、`(18.277,-43.630)`、
  `(14.844,-42.862)`；
- 规划结果：3 条清扫线、6 个状态机分段，预计总时间 117.663 s；
- Qt 生效参数：清扫宽度 1.20 m、间距 1.14 m、前进 1.10 m/s、倒车 0.30 m/s、
  最大角速度 0.30 rad/s、线加速度 1.00 m/s²、角加速度 0.70 rad/s²；
- 车辆参数：轴距 0.65 m、最小转弯半径 1.35 m；
- Hybrid TEB 前视 2.0 m；连续 3 个采样横向偏差超过 0.35 m或航向偏差超过
  0.55 rad 时事件触发重规划；
- 实验速度为操作员 Qt 当前值，不属于不高于 0.30 m/s 的低速安全验收口径。

## 时间线与运动结果

| 阶段 | CST 时间 | 耗时 | 结果与路径形状 |
| --- | --- | ---: | --- |
| 首线入场 | 03:14:50.929--03:15:54.250 | 63.321 s | Navfn + TEB 完成；末段有前进/倒车交替调姿，入口以终端 -0.105 m、横向 0.272 m、航向 13.0° 被有向边界接受。此项不是线间转场。 |
| 第 1 条清扫线 | 03:15:54.250--03:16:03.729 | 9.479 s | 完成；实走 7.864/计划 7.456 m，横向 0.014 m、航向 1.2°，进度 105.5%。 |
| 线 1 -> 线 2 | 03:16:03.729--03:16:38.445 | 34.716 s | 完成，但明显低效。路径依次被重算为 3.90 m（F/R/F，2 次换向）、3.00 m（3 段）、3.30 m（R/F）、2.70 m（F/R）、0.60 m（R）；实际提交 5 个固定档位 action，并在进入下一条前累计发生 4 次方向切换。每个实测 cusp 的缓存接回弦角分别超限 15.0°、33.3°、32.1°、10.0°（门限 5.7°），所以都重算到最终入口。 |
| 第 2 条清扫线 | 03:16:38.445--03:16:48.721 | 10.276 s | 完成；实走 8.367/计划 7.901 m，横向 0.005 m、航向 2.7°，进度 105.9%。 |
| 线 2 -> 线 3 | 03:16:48.721--约 03:17:37.7 | 约 49.0 s | 未完成并自动暂停。先生成 3.90 m F/R/F；首个 F 段约 5.3 s 后被接受。随后从实测 cusp 重算为 3.30 m R/F；R 段运行约 11 s 后持续停顿并偏离，进入异常恢复。 |

任务暂停时状态为 `segment=5/6`、`blocked_segments=1`，覆盖估算
17.740/28.004 m²（63.35%）。定位始终为 `LOCALIZED`；看门狗保持 `fresh authorized
command`；CAN、底盘急停状态和三雷达避障状态均正常；暂停后 `/cmd_vel` 与实测里程计
速度均为零。

## 失败链路的原始证据

### 1. Hybrid A* 初始搜索和整段重算均成功

第二次转场先得到：

```text
coverage planned direct Hybrid connector:
3.90m, parts=3, final=yes, expansions=19563,
cost=13.47s, reverse=0.90m, switches=2
```

首个 cusp 的实测接回弦角为 12.7°，超过 5.7° 门限后，系统按当前设计从实测位姿到
原最终清扫入口重算，而不是只规划到下一个 cusp。该次搜索也成功：

```text
coverage planned direct Hybrid connector:
3.30m, parts=2, final=yes, expansions=1070,
cost=10.46s, reverse=1.20m, switches=1
```

因此不能把该阶段称为“Hybrid A* 算不出路径”。

### 2. TEB 固定倒车 action 是第一个执行故障点

`hybrid_teb_command_mux` 已正确装载 `gear=reverse`，在线 TEB 参数也确认为
`motion_direction_mode=-1`、`max_vel_x_backwards=0.30`、最小转弯半径 1.35 m。但在
03:17:01--03:17:05 之间，`move_base` 连续记录：

```text
TebLocalPlannerROS: optimized command violated the fixed-gear contract.
Resetting planner...
```

源代码在速度饱和前检查 TEB 原始纵向速度符号；正向候选不会被传给底盘，而是被置零并
清除当前优化带。因此安全门工作正确，但造成 R 段反复重置/停车。随后管理器连续 3 个
样本测到路径横向偏差 0.351 m（刚超过 0.35 m 门限，航向偏差仅 5.6°），取消当前精确
action generation 并从实时位姿到最终入口重规划。这个事件触发动作本身符合设计。

当前日志只能证明 TEB 软优化最终给出了错误符号，尚不能仅凭日志把内部诱因唯一归结为
障碍代价、终点姿态还是优化权重。新空旷区域对照将用于区分静态边界是否为主要诱因；
在修复前仍应保留固定档位硬门，不能为了让车辆继续运动而放行反号命令。

### 3. 异常恢复不是搜索失败，而是候选被策略拒绝

进入入口附近恢复后，Hybrid 每次返回同一条 2.10 m 候选：前进 1.80 m、倒车 0.30 m。
`coverage_manager.py` 对入口恢复设置总长不超过 4.00 m且累计前进不超过 1.20 m，以阻止
车辆大幅前进绕圈。该候选总长合格，但前进长度超限 0.60 m，于是在 Hybrid service 成功
返回后被管理器拒绝：

```text
entry-alignment Hybrid path was rejected as a forward detour:
total 2.10m, forward 1.80m, reverse 0.30m
```

原始状态文字把这一结果概括成 `no feasible detour`，容易误诊为 Hybrid 搜索失败。准确
含义应是“存在几何路径，但没有通过当前入口恢复策略的候选”。操作员两次人工恢复后，
起点/目标和规则未变化，均重复相同拒绝并再次暂停；继续点击恢复没有收益。

### 4. 次要现象

- `/scan` observation buffer 在多个 action 交接/规划窗口出现 0.39--2.09 s 不新鲜告警；
  本轮所有初始 Hybrid 与重算搜索仍成功，日志没有把最终暂停归因于碰撞或全局路径安全
  许可失效，因此它不是已证实的终止根因，但后续实验需继续统计。
- 第一轮已完成的线 1 -> 线 2 转场中，每个 cusp 都因接回弦角超限而重算。正常 cusp 没有
  被状态机直接当作异常，但实测交接精度使“缓存后缀复用”完全没有发生，造成 34.716 s
  的转场和 5 次路径生成，远慢于仿真结果，属于独立的效率问题。
- mux 的 `stale TF/TEB/path lease or unsafe planner permit` 告警主要出现在 action 交接及
  TEB 被固定档位门重置之后；它解释零速保持，不是 Hybrid 搜索返回失败的证据。

## 下一轮空旷区域的判定重点

1. 保持同一 release 与参数，避免把换区域对照混入算法修改；
2. 分别记录每个转场的初始 Hybrid 路径长度、档位序列、搜索扩展数、实走时间；
3. 统计 `fixed-gear contract` 违规次数、cusp 缓存接回成功率、事件重规划次数和零速累计；
4. 若空旷区倒车段仍出现反号候选，可排除当前地图边界是必要条件，优先修 TEB 固定倒车
   优化/初始化；
5. 若 TEB 跟踪正常但入口恢复仍只给出前进 1.80 m候选，需要调整候选生成/选择，使搜索
   主动寻找满足倒车优先约束的替代拓扑，而不是简单放宽前进上限并接受大幅绕行；
6. 不把人工重复“恢复”计为新的独立实验样本；起点未变化时它只验证失败的确定性。

## 第二轮空旷区域对照与清扫顺序

第二轮计划 ID 为 `74ba15961fa740bb9d4566ab9c5dfb69`，开始于
2026-09-04 03:29:07.196 CST。区域面积 41.485 m²，顶点为
`(15.153,-20.751)`、`(18.343,-21.029)`、`(18.320,-33.685)`、
`(14.783,-32.688)`；仍使用第一轮相同车辆、速度和规划参数。计划总时间 100.137 s，
共 3 条清扫线、6 个状态机分段。

规划清扫顺序如下；这是 `/coverage/markers` 中按管理器 route 顺序发布的有向端点，并在
`MODE_ENFORCED_SWEEP` action 到来时再次核对：

| 顺序 | 起点 -> 终点 | 长度 | 航向 | 实际结果 |
| ---: | --- | ---: | ---: | --- |
| 1 | `(15.464,-32.880) -> (15.839,-20.811)` | 12.075 m | +88.2°，由南向北 | 已按此方向执行并完成 |
| 2 | `(16.976,-20.910) -> (16.595,-33.198)` | 12.294 m | -91.8°，由北向南 | 未进入；线 1 -> 线 2 转场失败 |
| 3 | `(17.725,-33.517) -> (18.114,-21.009)` | 12.514 m | +88.2°，由南向北 | 未执行 |

实际顺序与计划没有发生重排：只执行了第 1 条线，随后在去往第 2 条线入口时暂停。第 1 条
线从 03:30:39.060 到 03:30:52.344，用时 13.284 s；实走/计划为
12.563/12.075 m，横向误差 0.033 m、航向误差 0.5°、完成进度 104.0%。任务暂停时为
`segment=3/6`，覆盖估算 14.460/41.485 m²（34.86%）。

首线 Navfn + TEB 入场过程中在 03:29:47.075 收到一次真实 VCU `0x23` 故障事件，原始
字节为 `[0x01 0x01 0x01]`，表示 TCU、左 ECU、右 ECU emergency 位同时置 1。覆盖管理器
正确取消目标并要求人工恢复；周期底盘 emergency 状态随后恢复正常，CAN 发送错误计数为
零。操作员恢复后首线正常完成，因此该瞬态硬件事件单独记录，不是后续 Hybrid 转场失败
的原因。

第一次线间转场的复现链路为：

1. 03:30:54.938 成功生成 3.90 m、F/R/F、倒车 0.90 m、2 次换向的 Hybrid 路径，
   搜索 66,246 次扩展；
2. 第一段 F 约 6.6 s后接受；实测 cusp 接回弦角 6.4°略超 5.7°门限，因此从实时位姿到
   最终第 2 条线入口整段重算；
3. 03:31:01.764 成功生成 3.30 m、R/F、倒车 1.20 m、1 次换向的路径，搜索 3,811 次
   扩展；
4. 倒车 action 中再次反复出现 `optimized command violated the fixed-gear contract`；
   约 9.0 s后横向偏差达到 0.361 m（门限 0.35 m，航向仅 1.4°），触发事件重规划；
5. 入口恢复先后找到 2.40 m（F 2.10/R 0.30）、2.10 m（全前进）和 2.10 m
   （F 1.80/R 0.30）候选，均因前进距离超过 1.20 m后置上限而拒绝；
6. 约 03:31:43 第一次自动暂停。操作员在同一位姿人工恢复一次后候选仍被同一规则拒绝，
   约 03:32:31 再次暂停，未产生新的有效运动样本。

第二轮对照把“空旷度不足”从主要根因降为次要环境因素：区域位置、线长和第一次恢复候选
都发生变化，但故障仍稳定出现在 cusp 后的固定倒车 TEB action，并以约 0.36 m横向偏差
结束。应优先分析 TEB 倒车带的初始化/终端姿态和软约束为何仍会输出正向候选；恢复阶段则
需要在 Hybrid 搜索/候选排序内显式寻找满足前进上限的替代拓扑，而不是把已找到但不合策略
的路径笼统显示为“无可行路径”。

## 证据位置

- J6M ROS 日志：
  `/map/autolabor_runtime/logs/dual_host/20260904_025941/9ec9324c-a7c9-11f1-a364-5e9bc489939f/`
- 覆盖状态机日志：上述目录的 `coverage_manager-13.log`；
- TEB/mux/传感器联合日志：上述目录的 `rosout.log` 与
  `hybrid_teb_command_mux-12.log`；
- 当前实现：`src/application/autolabor_coverage/scripts/coverage_manager.py`、
  `src/navigation_arena/forks/navigation/local_planner/teb/src/teb_local_planner_ros.cpp`；
- 本轮诊断期间未修改导航源码或远端 release。
