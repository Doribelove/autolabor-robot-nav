# BPU 离线实验

2026-09-06 12:39：常驻推理、缓存预处理、无损省略恒定 padding、解码/NMS 和 Qt 刷新优化
已完成；默认上限 30 Hz、无时长参数仍持续模式。实测独立相机 14.90/28.22 Hz（15/30 Hz
输入），原导航相机 15 Hz 配置未改。详见根 `FCOS_PERFORMANCE_20260906.md`。
本轮进入时导航已 inactive、J6M 已重启；仅做私有 RGB/Qt 测试，现全部测试进程已退出，
两导航服务仍 inactive。没有发送运动、启动完整导航、切换正式后端或发布导航 release。
保留主运动配置 true 和既有授权标记，候选 current 仍为 20260905_160700；下方运行态为历史。
新 worker 仅使用实验私有原生库/运行库；不改系统频率，不为提升占用重复处理同一帧。

23:21：已再次按用户要求随静态地图/Qt 冷重启恢复新的持续桥，旧会话按请求正常停止。
最新运行会话以候选根目录 `STATIC_QT_RESTART_20260905.md` 为准。

22:42：用户明确要求一直运行，伴随桥已默认持续模式，两端不再设置演示截止。
同一会话实测超过 696 秒/340 帧，当前保持运行；导航/Qt 未重启。
最新入口与状态见候选根 `CONTINUOUS_BPU_20260905.md`；下方十分钟限制为旧演示历史。
持续模式仍只显示、按可见性停算，不自动重启故障或改导航；日志仅轮转新会话的私有文件。

22:08 接续：用户随后明确授权静止完整导航启动，21:50 已完成 BPU 与完整导航的短时并行
零速观察。最新要求综合页显示 BPU，候选 Qt 已增加综合/BPU 两页的可见性订阅；冷启动、
真实综合页框图及 20 秒零速检查通过。当前候选导航/实验桥运行，桥约 22:16 到期。
以候选根目录 `FULL_NAV_BPU_OVERVIEW_20260905.md` 为最新交付记录。
下方禁止未授权自动重启仍适用于未来操作，但不撤销本次明确授权。不得运动或切换生产后端。
离开综合和 BPU 两个显示页或最小化窗口才会暂停预览；会话仍最长 600 秒，无自动重启。

21:16 更新：用户已授权启动显示并要求接入导航 Qt。已完成独立静止 Qt/RGB/BPU 测试，
当前均停止；新增显示代码在候选 Qt 包内，其余在本实验。真实验证和入口见
`QT_PREVIEW_RESULTS.md`。生产后端和导航 release 未变，不把显示测试当导航验收。
`start_qt_bridge.sh` 是将来完整候选导航已安全恢复后的可选伴随入口；该并行场景尚未实测，
不要为验证它自动重启存在既有安全暂停的完整栈。所有实验结果必须保持仅显示属性。

遵守上层候选版 AGENTS.md。用户已授权 BPU 迁移实施，但没有授权移动、系统依赖升级、
固件修改或绕过既有现场安全暂停。仅模型离线执行不涉及底盘。

所有命令经 `scripts/optimized.sh run`；模型、导出、工具链缓存、日志不得写回原工程。
不得默认启用实验后端。远端只写 `bpu_lab_20260905` 独立目录，模型不得写入导航 release。
不要运行厂商一键压力测试、写 sysfs、清空 dmesg 或改变调频设置。
若运行库/编译器不兼容，保存原始错误，不能通过改版本号、去除检查或升级系统来绕过。
