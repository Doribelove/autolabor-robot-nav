# 静止诊断续测与安全中止记录 — 2026-09-05

## 当前状态

**候选和原版导航均已停止；不能按旧记录误认为仍在运行。**
18:16:53 开始计划 20 分钟的分段静止观察，完成 5 个完整 60 秒窗口后，
第 6 段检测到左轮小幅非零反馈，于 18:22 执行候选统一停止入口。
NVIDIA 底盘/视觉/雷达进程已退出，随后 J6M 完成有归属验证的清理；
11311 无监听、候选远端 PID 目录为空，本项目 CAN 驱动已退出。
普通权限 fuser 未返回 CAN 占用 PID，但部分其他用户的进程 FD 不可读；sudo 非交互检查
需要密码而未执行。因此不能声称已完成全系统、跨用户的 CAN 独占证明，没有绕过权限。
未发送导航目标、初始位姿或非零速度，双运动放行仍为 false，原版没有被启动。
待现场确认车辆实际静止、轮速脉冲来源以及相机遮挡后再继续实机测试。

诊断目录名为 `validation/stability_20260905_1819/`，名称是采集批次标签；
真实起止时间应以 `start_time.txt`、JSON 和 bag 时间戳为准。

## 安全中止证据

`diagnostic_topics.bag` 保留 426.23 秒，其中后段包含停机过程，不全是健康运行时间。

- 15199 条 `/cmd_vel`，所有六个分量均为零。
- 左、右轮各 15449 条反馈；右轮全零。
- 左轮仅 4 条非零：18:22:05.573 为 -0.0123700、05.633 为 +0.0123700、
  06.227 为 -0.0123700、06.395 为 +0.0123700 m/s。
- 静止观察器的 0.01 m/s 保守判据被触发，没有放宽判据或丢弃这些样本。
- 驱动从 ECU CurrentSpeed 浮点报文乘以轮半径生成反馈；本轮未录原始 CAN 报文，
  不能确定是外力轻触、传感器/编码器抖动还是实际轮子转动，也不能断言整车发生位移。
- 完整停栈不是瞬时完成：服务使用既有 180 秒停止上限，随后继续执行远端清理。
  期间 NVIDIA 驱动已退出、最终命令此前为零，J6M 的后续 scan-stale 日志属于停机后缺流。
  没有把完整清理耗时称作急停响应时间，物理急停响应仍未实测。

证据：`minute_06.log`、`safety_stop.log`、`analysis.json`、`final_stop_state.log`。

## 定位和传感器发现

在首个轮速脉冲之前，bag 中 6139 条定位状态为：
LOCALIZED 5458、DEGRADED 319、ALIGNING 241、LOST 121。
状态中 RMSE 中位数 0.3429 m、最大 0.3539 m，原门限仍为 0.35 m。
日志确认出现质量拒绝，而不是在正常运行期间普遍扫描断流。
采集到的主要 TF 边各只有一个发布者，未发现 map/camera_init/body/base_link 多重 TF 权威。
对相邻成功匹配的点云做独立最近邻检查得到约 0.3473 m 的内点 RMSE，
仅支持“拟合误差确实处在边缘”，不代表精确复现同一次 ICP，也不能确定地图、初值或场景哪个有误。
没有改定位算法、初值或任何定位质量门限。

ZED USB 为 5000M 且权限正常。相机原始画面大部分是紧贴镜头的白色物体，
21 个深度统计样本中 0.3–15 m 有效像素比例为 15.7–21.0%，平均 18.3%。
这与启动日志“避开 1 m 内物体、增加纹理/亮度”的自标定警告相符，
但不等于证明相机硬件坏了。需要现场确认遮挡；不改变安装角度、标定或安全阈值。
本次 203 对 RGB/深度时间戳完全匹配，仅证明同步，不证明测距准确。
原始照片和深度保存在 `validation/input_inspection_before_20260905/`。

## 本轮唯一生产入口修复

修改 `src/scripts/robot_bringup/launch/zed2_camera.launch`：去掉多余的
`rgb/camera_info -> /fod_camera/camera_info` 重映射，保留 RGB 和深度图像别名。
安装的 ROS CameraPublisher 在图像重映射后自动派生 camera_info 端点；
额外映射会让无人使用的 RGB 灰度发布器也看到内参订阅者，触发灰度图读取/复制。
这一行为同时经本机安装库的隔离 fixture 验证，机制可对照
[ROS Noetic CameraPublisher 实现](https://github.com/ros-perception/image_common/blob/noetic-devel/image_transport/src/camera_publisher.cpp)。

实际修复前 203 帧 RGB 对应 538 条内参（传输队列可丢弃重复条目），校准内容只有一种，
因此本轮没有把重复内参误报为不同标定混用。
隔离 master 11489 上，同样 5 轮发布：旧映射 15 条内参、灰度需求为真；
新映射 10 条内参、灰度需求为假。RGB/深度内参端点和校准内容不变。
RGB 与深度仍分别发布内参，保留这两份合法配对；不是“所有重复消息归零”。

这一修改只在 NVIDIA 候选源码/本地构建中，尚未通过真实相机重启验证。
J6M 未重新部署，候选发布仍为 `20260905_160700`，原发布仍为 `20260905_122153`。
不宣称该修复已降低实机 CPU/GPU，也不把它称为自标定警告修复。
新增 4 项路由合同测试及 `scripts/check_camera_aliases.cpp` 安装库回归 fixture。

## 资源观察及测量干扰

以下是新版静止运行、有诊断订阅和 ToDesk 的快照，不是新旧整机 A/B，也不覆盖移动任务。
18:17:00–18:20:40 的窗口排除了后续采图探针造成的 CPU 尖峰：

| 指标 | NVIDIA | J6M |
|---|---:|---:|
| 全机 CPU 均值 / 中位数 | 33.91% / 33.70% | 16.25% / 16.35% |
| 全机 CPU 范围 | 31.0–38.8% | 14.3–18.5% |
| 可用内存中位数 | 44870.9 MiB | 5583.4 MiB |
| swap 使用 | 0 | 0（无 swap） |

18:17–18:22 的单进程 CPU 中位数（单核口径）：NVIDIA 检测 38.0%、Qt/RViz 29.9%、
ZED 28.9%；J6M FAST-LIO 17.0%、ICP 9.5%、move_base 4.0%。
同一 5 分钟 GPU 60 点采样均值 58.27%、峰值 96%，包含桌面、远程连接及诊断干扰，不能归因给某一节点。
拓展的全机 CPU 序列中有 94.2% 的尖峰：对应我新增采图探针 PID 3221346 使用库默认线程，
该探针单核口径达到 770.1%，不是导航进程突然增加的负载。原始记录保留，未隐藏或用来夸大优化收益。
已在该诊断脚本内设置 OpenCV/BLAS 单线程，不修改导航进程或系统环境。
保存样本上的离线复测确认 OpenCV 线程数为 1、21 轮图像/深度统计约 0.14 CPU 秒；
这不是与原实时采集等条件的性能 A/B。后续实机重测需再次核对探针开销。

没有调整主机 CPU 绑核、GPU 功耗、驱动、系统服务或桌面刷新率。
ToDesk 客户端操作体验尚未得到用户确认，本轮因安全中止未完成显示开关 A/B。

## 验证与后续安排

- NVIDIA 增量 Release 构建通过；949 项项目测试，0 错误/失败/跳过。
- 静态健康检查通过；10 项隔离测试及更新文档后的 31 项双机合同测试通过。
- 安装库 fixture 新旧两种情况通过；4 项新增路由合同测试已纳入 949 项。
- 原项目 1287 个文件/链接复核无变化，原 J6M 配置、管理脚本和地图/发布链接清单前后相同。
- 本轮没有完成 20 分钟静止验收、完整相机精度检查、修复后实机启动或新旧资源 A/B。
- 不启动新控制链，不做移动测试；先由现场确认车辆静止、轮速脉冲来源和镜头前遮挡。
- 现场确认后，可按原报告的隔离入口完整冷启动；必须由操作员重新给出真实初始位姿，
  不能复用旧位姿来假定机器人没有移动。随后重做连续静止观察和显示/相机负载对照。
- 切回原版仍按 `OPTIMIZATION_REPORT.md` 中的方法，但原版正常配置允许运动；
  当前安全条件未澄清前不要用切回原版来绕过停止。
