1，保存当前代码状态，提交到git，作为另外的820分支。

2，当前的MID360与二维雷达融合的逻辑可能存在问题，架构改为：建图时，不作MID360与二维雷达的融合，而是建三个地图：MID360建一个三维地图；仅前后两个二维雷达融合，共同建立一个二维地图；
在之前基础上，建图完成后，把三维地图中的点云截取和二维雷达高度相同的部分，与建好的二维地图作融合得到新的二维地图（后简称“二三维融合图”）
三个地图保存到指定目录（在项目根目录下建立一个大文件夹，每建立一个新地图，就在大文件夹下面建一个小文件夹，里面又分三个子文件夹，分别保存对应的三维图、二维图、二三维融合图及配置文件）；
move_base启动时，定位方式：只用fastlio重定位算法，结合之前建立的三维地图作匹配定位，而原先建立的二维地图或二三维融合图只用于move_base的静态地图的加载。

3，qt界面在点击“录包”后，并不建立地图，而只是录入相关的话题数据，并保存到项目根目录下面的特定文件夹（没有可以建一个）；另外加一个“录入静态地图”的功能按钮，
点击后，开始建立2中说的前两个地图，还有一个“结束静态地图录入”的按钮，点击后则结束并将结果保存（按2中所说）

4，由于现在增加了静态地图的选项，因此双机一键启动脚本及命令需要作一些修改：在原本基础上，增加可选参数。默认为无全局地图形式，启动后效果和原来一样（用纯fastlio定位）；
若需要指定地图参数，就按照2中所示，参数给为新地图的文件夹目录，启动后按照fastlio重定位方式+加载静态地图，还有，在这种模式下，qt端‘录入静态地图’按钮变灰色，不可用，
另外，在此模式下，qt端需要显示静态地图、障碍层、膨胀层即全局路径。

5，因为现在的逻辑变了，qt部分可能得作修改，请你仔细阅读源码中的qt部分，确定如何修改。

6,请你仔细查看源代码中相关逻辑，如果发现上述需求有不清楚或者不周到、不正确的地方，请你指出，然后我们一同讨论确认。
















